name: Web CI

on:
  push:
    paths:
      - "apps/web/**"
      - "packages/**"
      - "scripts/check-web-structure.mjs"
      - ".github/workflows/web-ci.yml"
  pull_request:
    paths:
      - "apps/web/**"
      - "packages/**"
      - "scripts/check-web-structure.mjs"
      - ".github/workflows/web-ci.yml"

permissions:
  contents: read

jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck web
        run: pnpm --filter fresherflow-web typecheck

      # Enforces apps/web/AGENTS.md: dead shims 0, no back-edges, no route-private
      # leaks, no ui -> lib leaks. The count can only go down.
      - name: Check web structure
        run: pnpm --filter fresherflow-web check:structure:gate

      - name: Build web
        run: pnpm --filter fresherflow-web build

      - name: Trigger Vercel Deploy Hook
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
