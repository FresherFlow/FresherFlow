{
  "name": "fresherflow-firebase-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22 || >=24.0.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
